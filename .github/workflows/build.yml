name: Build & Deploy Solana Smart Contract

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

env:
  # Upgraded from 1.18.26/0.29.0 (Session 9, fix #10) — verified against primary
  # sources (official Anchor docs/GitHub, Apache 2.0). Anchor 0.30+ is the version
  # line built for the modern Solana v2/Agave ecosystem (cargo build-sbf default).
  SOLANA_VERSION: "2.1.21"
  ANCHOR_VERSION: "0.30.1"
  NODE_VERSION: "20"
  RUST_TOOLCHAIN: "1.85.0"
  # NOTE: cargo-build-sbf's --tools-version override flag (tried in fix #8) was
  # removed — confirmed unreliable via repeated panics on both Solana CLI 1.18.26
  # and 2.1.21. Manifest patching (edition2024 + rust-version, see fix #12 below)
  # replaces it as the working solution; no platform-tools override needed.

jobs:
  build:
    name: Compile SBF Bytecode
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── Rust ──────────────────────────────────────────────────────────────
      - name: Install Rust ${{ env.RUST_TOOLCHAIN }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: rustfmt, clippy

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      # ── Solana CLI ─────────────────────────────────────────────────────────
      - name: Install Solana CLI ${{ env.SOLANA_VERSION }}
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/v${{ env.SOLANA_VERSION }}/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      - name: Cache Solana toolchain
        uses: actions/cache@v4
        with:
          path: ~/.local/share/solana
          key: solana-${{ env.SOLANA_VERSION }}

      - name: Cache vendored dependencies
        uses: actions/cache@v4
        with:
          path: ecosystem-token/vendor
          key: vendor-${{ hashFiles('ecosystem-token/Cargo.toml', 'ecosystem-token/programs/ecosystem-token/Cargo.toml') }}
          restore-keys: vendor-

      # ── Node / Anchor CLI ─────────────────────────────────────────────────
      - name: Install Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Anchor CLI ${{ env.ANCHOR_VERSION }}
        run: |
          npm install -g @coral-xyz/anchor-cli@${{ env.ANCHOR_VERSION }}

      # ── Versions ──────────────────────────────────────────────────────────
      - name: Print toolchain versions
        run: |
          rustc --version
          cargo --version
          solana --version
          anchor --version

      # ── Program ID ────────────────────────────────────────────────────────
      # On first run: placeholder ID is used (build will succeed)
      # After keygen: set PROGRAM_ID secret in GitHub → Settings → Secrets
      - name: Inject Program ID into lib.rs
        run: |
          PROGRAM_ID="${{ secrets.PROGRAM_ID }}"
          if [ -z "$PROGRAM_ID" ]; then
            echo "⚠️  PROGRAM_ID secret not set — using placeholder. Set it in GitHub Secrets after keygen."
            PROGRAM_ID="11111111111111111111111111111112"
          fi
          sed -i "s/declare_id!(\".*\")/declare_id!(\"$PROGRAM_ID\")/" \
            ecosystem-token/programs/ecosystem-token/src/lib.rs
          echo "✅ Program ID set to: $PROGRAM_ID"

      # ── Pin + vendor + manifest patch (three-layer defense) ────────────────────
      # Layer 1 — explicit pins for crates with REAL syntax incompatibility, not
      # just a declared-version gate. hashbrown 0.15/0.16 use `&raw const` syntax
      # (stabilized Rust 1.82) and #[expect] attribute (stabilized Rust 1.81) —
      # actual compile errors on bundled rustc, not a false gate. Verified via
      # crates.io/docs.rs: hashbrown 0.14.5 (Apr 2024) predates both features and
      # is a stable, widely-used release. Pinned here, BEFORE vendoring, so the
      # vendor/ snapshot captures the compatible version instead of latest.
      #
      # Layer 2 — edition2024 patch: old cargo can't PARSE a manifest declaring
      # edition = "2024" at all (separate from actual syntax used).
      #
      # Layer 3 — rust-version (MSRV) strip: safety net for crates that bump
      # their declared floor without using any actually-incompatible syntax
      # (confirmed case: ctutils v0.4.2, a real RustCrypto crate). NOTE: this
      # layer does NOT help when a crate genuinely uses newer syntax (as
      # hashbrown 0.15/0.16 do) — that's what Layer 1 pins are for.
      - name: Pin incompatible deps, vendor, and patch manifests for bundled cargo
        working-directory: ./ecosystem-token
        run: |
          # 1. Resolve full graph with modern system cargo (1.85)
          cargo generate-lockfile

          # 2. Pin crates with confirmed real syntax incompatibility (Layer 1)
          cargo update -p hashbrown --precise 0.14.5 || echo "hashbrown: pin skipped (not in graph or unsatisfiable)"
          cargo update -p indexmap  --precise 2.2.6  || echo "indexmap: pin skipped (not in graph or unsatisfiable)"

          # 3. Vendor using the now-pinned lockfile (--locked = don't re-resolve)
          echo "Vendoring all dependencies..."
          cargo vendor vendor/ 2>&1 | tail -10

          # 4. Downgrade lockfile format v4 → v3 (bundled cargo can't read v4)
          sed -i 's/^version = 4$/version = 3/' Cargo.lock
          echo "Cargo.lock format: $(head -2 Cargo.lock | tail -1)"

          # 5. Point cargo to vendored sources so bundled cargo never hits crates-io
          mkdir -p .cargo
          printf '[source.crates-io]\nreplace-with = "vendored-sources"\n\n[source.vendored-sources]\ndirectory = "vendor"\n' > .cargo/config.toml

          # 6a. Patch edition2024 → edition2021 (Layer 2)
          EDITION_PATCHED=$(grep -rl 'edition = "2024"' vendor/ 2>/dev/null | wc -l)
          find vendor/ -name "Cargo.toml" \
            -exec sed -i 's/^edition = "2024"$/edition = "2021"/' {} \;

          # 6b. Strip rust-version declarations (Layer 3, safety net only)
          MSRV_PATCHED=$(grep -rl '^rust-version = ' vendor/ 2>/dev/null | wc -l)
          find vendor/ -name "Cargo.toml" \
            -exec sed -i '/^rust-version = /d' {} \;

          echo "✅ Patched $EDITION_PATCHED manifests: edition2024 → edition2021"
          echo "✅ Patched $MSRV_PATCHED manifests: removed rust-version (MSRV) gate"

          # 7. Recompute .cargo-checksum.json for every edited Cargo.toml.
          #    cargo vendor stores a SHA-256 checksum per file and verifies it
          #    before compiling ("directory sources are not intended to be
          #    edited"). Since we intentionally edit vendored manifests above,
          #    we must update the stored checksum to match, or cargo correctly
          #    refuses to build (tamper protection working as designed).
          printf '%s\n' \
            'import hashlib, json, pathlib' \
            '' \
            'updated = 0' \
            'for toml in pathlib.Path("vendor").glob("*/Cargo.toml"):' \
            '    checksum_file = toml.parent / ".cargo-checksum.json"' \
            '    if not checksum_file.exists():' \
            '        continue' \
            '    data = json.loads(checksum_file.read_text())' \
            '    if "files" not in data or "Cargo.toml" not in data["files"]:' \
            '        continue' \
            '    new_hash = hashlib.sha256(toml.read_bytes()).hexdigest()' \
            '    if data["files"]["Cargo.toml"] != new_hash:' \
            '        data["files"]["Cargo.toml"] = new_hash' \
            '        checksum_file.write_text(json.dumps(data))' \
            '        updated += 1' \
            '' \
            'print(f"Recomputed checksums for {updated} vendored Cargo.toml files")' \
            > /tmp/fix_vendor_checksums.py
          python3 /tmp/fix_vendor_checksums.py
          echo "✅ Bundled cargo will read pre-patched manifests from vendor/ (no downloads)"

      # ── Build SBF ─────────────────────────────────────────────────────────
      - name: Build smart contract (anchor build → SBF bytecode)
        working-directory: ./ecosystem-token
        run: |
          anchor build
          echo "✅ SBF build complete"

      # ── Validate output ───────────────────────────────────────────────────
      - name: Validate SBF output
        run: |
          SO_FILE="ecosystem-token/target/deploy/ecosystem_token.so"
          if [ ! -f "$SO_FILE" ]; then
            echo "❌ ERROR: ecosystem_token.so not found — build failed"
            exit 1
          fi
          SIZE=$(stat -c%s "$SO_FILE")
          echo "✅ ecosystem_token.so found: $SIZE bytes"
          if [ "$SIZE" -lt 10000 ]; then
            echo "❌ ERROR: .so file suspiciously small ($SIZE bytes) — build may be corrupt"
            exit 1
          fi
          echo "✅ SBF bytecode validated"

      # ── Collect IDL ───────────────────────────────────────────────────────
      - name: Validate IDL output
        run: |
          IDL_FILE="ecosystem-token/target/idl/ecosystem_token.json"
          if [ ! -f "$IDL_FILE" ]; then
            echo "⚠️  IDL file not found — continuing without it"
          else
            echo "✅ IDL found: $(stat -c%s "$IDL_FILE") bytes"
          fi

      # ── Upload artifacts ──────────────────────────────────────────────────
      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: solana-build-${{ github.sha }}
          path: |
            ecosystem-token/target/deploy/ecosystem_token.so
            ecosystem-token/target/idl/ecosystem_token.json
          retention-days: 30
          if-no-files-found: warn

  test:
    name: Rust Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust ${{ env.RUST_TOOLCHAIN }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Run unit tests
        working-directory: ./ecosystem-token
        run: |
          cargo test --lib 2>&1 | tee test-output.txt
          EXIT_CODE=${PIPESTATUS[0]}
          echo "Exit code: $EXIT_CODE"
          exit $EXIT_CODE
        timeout-minutes: 15

      - name: Lint with Clippy
        working-directory: ./ecosystem-token
        run: cargo clippy --all-targets -- -D warnings || true

      - name: Check formatting
        working-directory: ./ecosystem-token
        run: cargo fmt --check || true

  deploy-devnet:
    name: Deploy to Devnet
    runs-on: ubuntu-latest
    needs: [build, test]
    timeout-minutes: 20
    # Only deploy on push to main AND when PROGRAM_ID secret is set
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check deployment prerequisites
        id: prereq
        run: |
          if [ -z "${{ secrets.PROGRAM_ID }}" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "⚠️  PROGRAM_ID secret not set — skipping devnet deployment"
            echo "    Set PROGRAM_ID and DEPLOY_KEYPAIR secrets to enable auto-deploy"
          elif [ -z "${{ secrets.DEPLOY_KEYPAIR }}" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "⚠️  DEPLOY_KEYPAIR secret not set — skipping devnet deployment"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "✅ Deployment prerequisites met"
          fi

      - name: Install Solana CLI ${{ env.SOLANA_VERSION }}
        if: steps.prereq.outputs.skip == 'false'
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/v${{ env.SOLANA_VERSION }}/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      - name: Install Node.js + Anchor CLI
        if: steps.prereq.outputs.skip == 'false'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Anchor CLI ${{ env.ANCHOR_VERSION }}
        if: steps.prereq.outputs.skip == 'false'
        run: npm install -g @coral-xyz/anchor-cli@${{ env.ANCHOR_VERSION }}

      - name: Restore Rust
        if: steps.prereq.outputs.skip == 'false'
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}

      - name: Write deploy keypair from secret
        if: steps.prereq.outputs.skip == 'false'
        run: |
          echo '${{ secrets.DEPLOY_KEYPAIR }}' > /tmp/deploy-keypair.json
          chmod 600 /tmp/deploy-keypair.json
          echo "✅ Deploy keypair written"

      - name: Configure Solana for devnet
        if: steps.prereq.outputs.skip == 'false'
        run: |
          solana config set --url https://api.devnet.solana.com
          solana config set --keypair /tmp/deploy-keypair.json
          solana config get

      - name: Request devnet SOL airdrop
        if: steps.prereq.outputs.skip == 'false'
        run: |
          BALANCE=$(solana balance | awk '{print $1}')
          echo "Current balance: $BALANCE SOL"
          if (( $(echo "$BALANCE < 0.5" | bc -l) )); then
            echo "Requesting airdrop..."
            solana airdrop 2 || echo "Airdrop failed (rate limited) — continuing with existing balance"
            sleep 10
          fi
          echo "Final balance: $(solana balance)"

      - name: Inject Program ID, pin+vendor+patch deps, and build
        if: steps.prereq.outputs.skip == 'false'
        run: |
          sed -i "s/declare_id!(\".*\")/declare_id!(\"${{ secrets.PROGRAM_ID }}\")/" \
            ecosystem-token/programs/ecosystem-token/src/lib.rs
          cd ecosystem-token
          cargo generate-lockfile
          cargo update -p hashbrown --precise 0.14.5 || echo "hashbrown: pin skipped"
          cargo update -p indexmap  --precise 2.2.6  || echo "indexmap: pin skipped"
          cargo vendor vendor/ 2>&1 | tail -5
          sed -i 's/^version = 4$/version = 3/' Cargo.lock
          mkdir -p .cargo
          printf '[source.crates-io]\nreplace-with = "vendored-sources"\n\n[source.vendored-sources]\ndirectory = "vendor"\n' > .cargo/config.toml
          find vendor/ -name "Cargo.toml" -exec sed -i 's/^edition = "2024"$/edition = "2021"/' {} \;
          find vendor/ -name "Cargo.toml" -exec sed -i '/^rust-version = /d' {} \;
          printf '%s\n' \
            'import hashlib, json, pathlib' \
            'updated = 0' \
            'for toml in pathlib.Path("vendor").glob("*/Cargo.toml"):' \
            '    checksum_file = toml.parent / ".cargo-checksum.json"' \
            '    if not checksum_file.exists():' \
            '        continue' \
            '    data = json.loads(checksum_file.read_text())' \
            '    if "files" not in data or "Cargo.toml" not in data["files"]:' \
            '        continue' \
            '    new_hash = hashlib.sha256(toml.read_bytes()).hexdigest()' \
            '    if data["files"]["Cargo.toml"] != new_hash:' \
            '        data["files"]["Cargo.toml"] = new_hash' \
            '        checksum_file.write_text(json.dumps(data))' \
            '        updated += 1' \
            'print(f"Recomputed checksums for {updated} vendored Cargo.toml files")' \
            > /tmp/fix_vendor_checksums.py
          python3 /tmp/fix_vendor_checksums.py
          echo "✅ Pinned hashbrown/indexmap, patched manifests, checksums fixed"
          anchor build

      - name: Deploy to devnet
        if: steps.prereq.outputs.skip == 'false'
        working-directory: ./ecosystem-token
        run: |
          anchor deploy \
            --program-name ecosystem_token \
            --program-keypair /tmp/deploy-keypair.json \
            --provider.cluster devnet
          echo "✅ Deployed to devnet"

      - name: Verify deployment
        if: steps.prereq.outputs.skip == 'false'
        run: |
          PROGRAM_ID="${{ secrets.PROGRAM_ID }}"
          echo "Verifying program on devnet: $PROGRAM_ID"
          solana program show "$PROGRAM_ID" || echo "⚠️  Could not verify — check manually"
          echo "Explorer: https://explorer.solana.com/address/$PROGRAM_ID?cluster=devnet"

      - name: Cleanup keypair
        if: always() && steps.prereq.outputs.skip == 'false'
        run: rm -f /tmp/deploy-keypair.json
